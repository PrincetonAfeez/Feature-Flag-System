#
# Runtime dependency lock (Django closure), pinned for reproducible installs.
# Regenerate after changing [project.dependencies] in pyproject.toml:
#
#   python -m pip install pip-tools
#   python -m piptools compile pyproject.toml -o requirements.txt --strip-extras
#
# Dev/test tooling: pip install -e ".[dev]"
#
# This file is autogenerated by pip-compile with Python 3.14
# by the following command:
#
#    pip-compile --output-file=requirements.txt --strip-extras pyproject.toml
#
asgiref==3.11.1
    # via django
django==5.2.14
    # via feature-flag-system (pyproject.toml)
sqlparse==0.5.5
    # via django
tzdata==2026.1
    # via django
